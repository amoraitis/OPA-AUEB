<Page
    x:Class="AuebUnofficial.Viewers.AnouncementsEclass"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AuebUnofficial.Viewers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Name="CoursesTemplate">
            <StackPanel Orientation="Vertical" >

                <TextBlock Text="{Binding Name}" TextWrapping="Wrap" MaxLines="2"/>
                <TextBlock Text="{Binding Id}"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Width="Auto" Height="Auto" x:Name="anoun"/>
            <TextBlock x:Name="testing"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2" BorderThickness="1" Margin="5">
            <ItemsControl Name="Display1">
                <ListView ItemTemplate="{StaticResource CoursesTemplate}" x:Name="ListView1" HorizontalAlignment="Left"/>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
